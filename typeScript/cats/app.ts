import { square, IP, add } from "./math";
import { allCats } from "./shelter/index";

console.log(IP);
console.log(add(10, 5));
console.log(square(IP));

console.log("Required an entire directory", allCats);
